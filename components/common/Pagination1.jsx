/**
 * Pagination1 — previously rendered a page-loader.gif spinner.
 * Replaced with null: all loading states now use skeleton UIs instead.
 * Kept as a component (not deleted) so the many existing imports don't break.
 */
export default function Pagination1() {
  return null;
}
